@model IEnumerable<CarParkingSystem.Models.ParkingSpace>
@{
    ViewBag.Title = "CheckIn";
}


<ul>
    @{
        int i = 0;
        foreach (var item in Model)
        {
            if (i < 9)
            {
                if (item.Status == true)
                {
                <a href="../../CheckIn/CheckInFinal?SId=@item.Id" class="btn btn-primary">@item.PSName</a>
                    i++;
                }
                else
                {
                    <a href="#" class="btn btn-danger">@item.PSName</a>
                    i++;
                }
            }
            else
            {
                if (item.Status == true)
                {
                <br /> 
                <a href="../../CheckIn/CheckInFinal?SId=@item.Id" class="btn btn-primary">@item.PSName</a>
                    i = 1;
                }
                else
                {
                    <br />
                    <a href="#" class="btn btn-danger">@item.PSName</a>
                    i = 1;
                }
            }

        }
        }
</ul>


